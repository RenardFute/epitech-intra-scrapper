{
  "name": "intra-epitech-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "prestart": "rimraf -rf dist && tsc -p . && cp-cli .env dist/.env",
    "start": "cd dist && node --experimental-json-modules index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "mysql": "^2.18.1",
    "puppeteer": "^21.5.2"
  },
  "devDependencies": {
    "@types/mysql": "^2.15.24",
    "@types/node": "^20.9.2",
    "cp-cli": "^2.0.0",
    "rimraf": "^5.0.5"
  }
}
